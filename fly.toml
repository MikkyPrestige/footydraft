app = "footydraft"

[build]
  dockerfile = "Dockerfile"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"

[mounts]
  source = "agent_data"
  destination = "/app/data"

[processes]
  app = "bash start.sh"

[deploy]
  strategy = "immediate"
